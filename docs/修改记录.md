


我现在需要具体实现发送测试指令的功能。我会将需要发送的指令写成一个配置文件。初步考虑格式如下：
///////////////////////////////////////////////////////////////////////////////////
“对指令功能的文字描述” ：AA 55 55 AA 88 88 00 10 00 00 00 00 CF 10 00 01 00 00 0D EE
“对指令功能的文字描述” ：AA 55 55 AA 88 88 00 10 00 00 00 00 CF 10 00 02 00 00 0D EE
……
///////////////////////////////////////////////////////////////////////////////////
程序中要能够读取配置文件中的指令描述和十六进制数据，之后通过软件接口和硬件接口发送数据
程序在发送指令后，需要准备好接收回传的硬件测试结果。对结果进行处理，之后再根据处理结果更新UI状态，之后再发送下一条数据，如此循环
这一块功能是程序的核心功能，把他放在tasks文件夹中，如果一个test manager文件把逻辑全塞进去比较混乱，就开新的文件，这个你分析一下怎么做合适，也不一定要开新文件，关键是让整个代码工程清晰，方便修改。


我需要你看一下这个工程，然后帮我描述一下。当我运行工程，并点击开始测试按钮时，会依次发生什么，涉及到哪些文件，哪些类会在工作，每个类的主要功能是啥，每个函数的主要功能是啥。

我现在希望发送指令+接收指令与处理数据+更新UI是两个不同的线程，即发送指令一定要等上一个指令的回传结果，之后才能发送下一个指令，我可以有一个帧作为回传结束标志。然后接收的数据可以进入一个缓存池，缓存池中的数据可以被处理线程池中的线程处理，处理完成后，根据处理结果更新UI状态。这样发送接收与处理数据就可以并行进行，不会阻塞UI线程。但需要注意的是，我还希望已发送的指令和接收的指令也要实时更新在UI上，然后处理数据的处理结果也更新在UI上。

我有完整的发送与接收帧的定义，是一张excel表，前半部分是发送的指令，后半部分是接收的指令。我需要根据这张表，在程序中实现发送指令和接收指令的功能。我是想把这两张表的内容转换成标准的yaml文件，这样程序可以直接读取分析。excel表我作为附件发给你可行吗
